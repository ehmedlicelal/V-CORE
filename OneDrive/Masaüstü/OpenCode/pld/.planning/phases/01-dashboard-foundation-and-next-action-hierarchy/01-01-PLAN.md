---
phase: "01-dashboard-foundation-and-next-action-hierarchy"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/StudentDashboard.jsx
  - client/src/pages/StudentDashboard.css
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - PRAC-01
user_setup: []
must_haves:
  truths:
    - "Student lands on the dashboard and sees one clearly dominant next-action card before any secondary sections."
    - "Student can start AI practice from a persistent top-area action when no session needs immediate attention."
    - "Student no longer sees the welcome banner, stats strip, or other stats-first summary above the main action area."
    - "Student sees calm empty states for session areas that explain what to do next instead of dead-end placeholders."
  artifacts:
    - path: "client/src/pages/StudentDashboard.jsx"
      provides: "Next-action hierarchy, practice fallback placement, and helpful empty-state rendering"
      min_lines: 200
      contains: "navigate('/practice')"
    - path: "client/src/pages/StudentDashboard.css"
      provides: "Calm dashboard hierarchy styling for the top action area and empty states"
      min_lines: 150
      contains: ".student-empty-state"
  key_links:
    - from: "client/src/pages/StudentDashboard.jsx"
      to: "/practice"
      via: "top-area CTA routing"
      pattern: "navigate\\('/practice'\\)"
    - from: "client/src/pages/StudentDashboard.jsx"
      to: "getSessions/getJoinableSessions"
      via: "derived next-action data"
      pattern: "getSessions|getJoinableSessions"
    - from: "client/src/pages/StudentDashboard.jsx"
      to: "client/src/pages/StudentDashboard.css"
      via: "page-scoped class names for hierarchy and empty-state presentation"
      pattern: "StudentDashboard\\.css"
---

<objective>
Rebuild the top of the student dashboard around a single next action, preserve AI practice as the fallback action, and replace abrupt placeholders with calm guidance.

Purpose: Deliver the Phase 1 hierarchy shift without spilling into the later session-detail, announcements, or PLD reliability phases.
Output: Updated `StudentDashboard.jsx` structure and view logic plus matching `StudentDashboard.css` hierarchy and empty-state styling.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dashboard-foundation-and-next-action-hierarchy/01-CONTEXT.md
@client/src/pages/StudentDashboard.jsx
@client/src/pages/StudentDashboard.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive one primary next action and reorder the top-of-page structure</name>
  <files>client/src/pages/StudentDashboard.jsx</files>
  <action>Replace the current welcome banner, stats row, and promotional AI banner stack with a single top-area hierarchy. Compute one primary next action from the data already loaded in the page: prefer an active session that can be entered now, otherwise use the most relevant joinable/upcoming session, and otherwise fall back to AI practice. Keep the welcome copy short inside this primary card instead of as a separate banner. Preserve existing routes and join behavior, and do not introduce new API dependencies or expand into later-phase session state modeling.</action>
  <verify>From `client/`, run `npx eslint src/pages/StudentDashboard.jsx` and confirm the top-level render starts with one next-action area instead of separate welcome/stats/banner blocks.</verify>
  <done>The page has one dominant next-action card at the top, session/practice priority is data-driven, and no stats-first strip remains above primary actions.</done>
</task>

<task type="auto">
  <name>Task 2: Keep practice available and convert session placeholders into helpful empty states</name>
  <files>client/src/pages/StudentDashboard.jsx</files>
  <action>Add a quieter practice fallback card directly under the primary next-action card whenever practice is not already the primary action. Rewrite the session-area empty states so they remain visible, calm, and action-oriented: if there is nothing urgent, direct the student toward practice or the next useful session-oriented step instead of showing terse dead-end copy. Keep announcements and PLD preferences present but below the primary action area so Phase 1 stays focused on hierarchy rather than secondary-surface redesign.</action>
  <verify>Inspect the rendered branches in `StudentDashboard.jsx` to confirm there is always either a primary practice CTA or a secondary fallback practice CTA, and that both session sections still render with explicit empty-state cards when their lists are empty.</verify>
  <done>Practice stays reachable near the top of the page, empty states give the student a next step, and the dashboard never collapses into blank or dead-end session sections.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle the dashboard for a calm, airy hierarchy without promotional intensity</name>
  <files>client/src/pages/StudentDashboard.css</files>
  <action>Replace the current loud banner- and gradient-heavy treatments with page-scoped styles that support one clear top action, a quieter secondary practice card, and simple empty-state cards. Remove styles that exist only for the old welcome banner and stats row, tighten the section spacing so the first screen feels quieter, and add responsive rules that keep the new CTA ordering readable on mobile. Stay within the existing CSS file rather than introducing a new styling system.</action>
  <verify>From `client/`, run `npm run build` to confirm the updated JSX and CSS compile cleanly, then review the stylesheet to verify the old welcome/stats selectors are no longer needed by the top of the page.</verify>
  <done>The top area reads as one primary card plus one subdued fallback card, the first screen is less noisy than the current dashboard, and empty states match the calmer visual tone on desktop and mobile.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] From `client/`, `npx eslint src/pages/StudentDashboard.jsx` passes
- [ ] From `client/`, `npm run build` succeeds
- [ ] `StudentDashboard.jsx` renders a single top next-action hierarchy before announcements, preferences, or other secondary sections
- [ ] `StudentDashboard.jsx` still exposes a near-top practice CTA and shows calm empty-state guidance for missing session data
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The dashboard top area now communicates one primary student action instead of multiple competing banners
- AI practice remains immediately reachable near the top without competing with an urgent session action
- Empty states guide the student toward a useful next step instead of ending the workflow
  </success_criteria>

<output>
After completion, create `.planning/phases/01-dashboard-foundation-and-next-action-hierarchy/01-01-SUMMARY.md`
</output>
